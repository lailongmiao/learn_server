-- Add migration script here
-- create table users(
--     id serial primary key,
--     username varchar(255) not null unique,
--     email varchar(255) not null unique,
-- );
 INSERT INTO users  (id,username,email) values(3,'test3','test3@example.com');
